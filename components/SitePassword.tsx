'use client'

import { useState, useEffect } from 'react'

export default function SitePassword({ children }: { children: React.ReactNode }) {
  // Proteção por senha removida: acesso livre
  return <>{children}</>;
}
